# 🏆 睡前AI伴睡精灵 (Sleep-Companion AI) - 黑客松路演参考文档

> **项目名称**：睡前AI伴睡精灵 (内部代号：OPC-01)
> **参赛团队**：OPC 创始团队（PM & 研发）
> **开发周期**：4天极限开发
> **开发模式**：0代码、氛围感编程 (AI Agent 驱动)
> **项目定位**：下一代 AI 床头夜灯（情绪疗愈智能硬件）

---

## 一、 演讲开场：一句话抓住眼球 (Elevator Pitch)

**【PM 话术建议】**
> “各位评委好，我们是未来的 **OPC 公司**创始团队。
> 今天我们带来的项目是——一款能够‘听懂你情绪’的**全语音交互睡前AI伴睡精灵**。
> 值得一提的是，这个具备完整前后端、流式语音打断、情绪识别的复杂全栈项目，是我们两人团队在短短 **4天** 内，完全采用**‘0代码、氛围感编程’**打造而成的！”

---

## 二、 痛点与解决方案 (Problem & Solution)

### 1. 痛点分析：为什么现代人睡不好？
*   **屏幕依赖与睡前焦虑**：大多数人在睡前难以放下手机，屏幕蓝光和无尽的信息流加剧了失眠与焦虑。
*   **传统助眠产品的呆板**：白噪音或固定的睡前播客缺乏互动性，它们是在“广播”，而不是“陪伴”，无法根据用户当下的**情绪状态**进行动态调整。

### 2. OPC 的解决方案：闭环的语音交互生态与实体夜灯
*   **放下屏幕，回归纯粹**：纯语音交互（Voice-in, Voice-out），做成床头夜灯的形态，让用户闭着眼睛也能获得陪伴。这是物理层面的“断舍离”。
*   **情绪疗愈引擎**：通过分析用户的语气和用词，动态调整故事走向和讲述声音。焦虑时安抚，放松时陪伴。

---

## 三、 核心亮点与技术“秀肌肉” (Core Highlights - 拿奖关键)

在这一部分，向评委展示你们对前沿 AI 技术的深刻理解和巧妙的架构设计。
*(注：以下内容结合了硬核技术原理与通俗比喻，既能应对技术评委的提问，也方便 PM 在台上通俗演绎)*

### 1. 全栈 AI 模型矩阵：阿里云通义千问全家桶深度集成
为了实现毫秒级响应和极高的系统集成度，我们将整个 AI 链路构建在**阿里云百炼平台**之上，精细化调用了四款不同的专业模型：
*   **【大脑-慢思考】：`qwen-plus` (大语言模型)**
    *   **角色**：资深说书人与心理咨询师。
    *   **职责**：专门负责生成具备文学性、逻辑性和疗愈感的长篇睡前故事。它被置于后置流程，只在需要深度创作时才被唤醒。
*   **【小脑-快思考】：`qwen-turbo` (轻量级语言模型)**
    *   **角色**：前台接待与意图解析器。
    *   **职责**：利用其极低的延迟，在用户话音刚落的瞬间完成两件事：一是执行 `analyzeEmotion` 提取情绪标签（如：焦虑/愉悦）；二是执行 `parseVoiceParams` 解析自然语言指令（如听懂“换个温柔的女声”）。
*   **【耳朵】：`qwen3-asr-flash` (自动语音识别模型)**
    *   **角色**：同声传译。
    *   **职责**：接收前端发送的 Base64 音频流，在百毫秒内将其精准转化为文本，作为整个对话系统的输入引擎。
*   **【嘴巴】：`qwen3-tts-flash` (流式语音合成模型)**
    *   **角色**：多变配音员。
    *   **职责**：将 `qwen-plus` 生成的流式文本实时转为高品质的音频流，支持 49 种不同音色（如默认的治愈系女声 Chelsie），并通过 Python 桥接推送到前端。

### 2. 极致的“大小模型路由”策略 (LLM Routing)
*   **技术架构**：采用 **LLM Routing** 模式。通过前置 `qwen-turbo` 负责 NLU（自然语言理解）与意图提取，后置 `qwen-plus` 负责长文本生成，以此平衡响应延迟与生成质量。
*   **通俗比喻**：就像公司里的“前台接待”和“资深专家”，分工明确，兼顾效率与质量。让 Turbo 处理简单的分类任务，既省成本又极速；让 Plus 专注创作，保证故事质量。

### 3. 毫秒级低延迟的流式交互 (Streaming)
*   **技术架构**：抛弃传统的 HTTP 请求-响应模型，采用 **全双工 WebSocket** 结合 **Server-Sent Events (SSE)** 的理念，打通了从 `LLM 流式文本 (Stream)` 到 `TTS 流式音频 (Audio Chunk)` 的端到端管道。
*   **通俗比喻**：传统的 AI 聊天是“发微信”（等写完再回），我们做的是“打电话”（一边想一边说，像流水一样源源不断）。
*   **底层实现**：利用 Node.js 的异步生成器 (`AsyncGenerator`) 处理 `qwen-plus` 的流式输出，同时通过 Python 桥接 (`child_process.spawn`) 调用 `qwen3-tts-flash` 接口，将文本切片实时合成为 `base64` 音频块推送至前端，实现近乎零等待的语音交互。

### 4. 动态状态机驱动的交互逻辑
*   **技术架构**：在后端实现了一个基于 **有限状态机 (Finite State Machine, FSM)** 的对话控制器，状态流转受到 `EmotionLabel` 和 `InteractionRounds` 的双重约束。
*   **通俗比喻**：给 AI 装了一个“剧本导航仪”，防止聊天跑题，让 AI 像个真正的心理咨询师一样掌控节奏。
*   **底层实现**：核心逻辑定义在 `InteractionManager` 中，状态包含 `INITIAL` -> `EMOTION_ASSESSMENT` -> `RECOMMENDATION` -> `STORYTELLING`。例如，如果模型判定用户处于焦虑状态，状态机会将其锁定在 `EMOTION_ASSESSMENT`（安抚阶段），直到检测到情绪缓和才会进入下一阶段。

---

## 四、 硬件愿景：下一代 AI 床头夜灯

**【PM 话术建议 - 配合展示模型演示】**
> “各位评委，大家刚才看到的网页端，其实只是我们产品的‘大脑’和‘灵魂’。
> **OPC 公司的真正野心，是打造下一代 AI 床头夜灯。**
> 
> 在这个项目中，我们已经在架构底层为硬件实体化做好了完全的准备：
> 1. **全双工 WebSocket 通信**：这意味着我们目前的架构可以直接无缝移植到 **ESP32** 或 **树莓派** 上，实现低功耗的硬件端流式语音交互。
> 2. **硬件级场景标记 (Scene Markers)**：在我们的后端大模型流中，隐藏着像 `[SCENE: fairy_tale]` 这样的控制指令。目前它在网页端控制的是背景白噪音的切换；而在未来的实体夜灯上，**大模型讲到森林时，夜灯会自动变绿并播放沙沙的树叶声；讲到篝火时，夜灯会变成温暖的呼吸橙色。**
> 
> 今天我们带来了一个展示模型。未来，这盏灯将替代手机，成为每个焦虑现代人床头的‘情绪守护者’。”

---

## 五、 商业价值与 OPC 公司的未来愿景 (Business & Future)

**【PM 话术建议】**
> “评委老师们，这不仅仅是一个 Hackathon 玩具，它是 **OPC 公司** 的第一款孵化产品。
> 我们通过这个项目证明了一件事：**产品经理 + AI 构成的‘氛围感编程’，已经可以完全取代传统外包和低效的初期开发团队。** 
> 
> **我们的商业模式：**
> 1. **To C 端**：以‘情绪疗愈+AI硬件’切入千亿级的睡眠健康/冥想市场（对标 Calm, Headspace 的下一代实体 AI 形态）。
> 2. **To B 端 (OPC 愿景)**：提炼本次 4 天极限开发的架构范式，OPC 将致力于打造‘基于 AI 意图驱动的快速应用生成器’，将这种开发能力产品化。”

---

## 附录：评委 Q&A 防御指南 (供 PM 备查)

**Q1：你们说自己是“0代码”，那这个复杂的系统是怎么搭建的？**
> **回答策略**：强调“氛围感编程”和“架构前置”。我们没有手写枯燥的代码，而是扮演了**架构师和产品经理**的角色。我们定义了核心的数据流（WebSocket）、API 契约以及大模型之间的协作关系（Turbo 负责 NLU，Plus 负责生成），然后通过 AI 编程助手精准下达 prompt 生成高质量代码。这展示了 AI 时代开发者角色的升维。

**Q2：市场上有很多讲故事的 AI，你们的壁垒在哪里？**
> **回答策略**：
> 1. **情绪路由机制**：我们不仅讲故事，我们还能实时分析用户的“喜怒哀乐”，动态改变故事基调和 TTS 的音色（比如识别到焦虑，自动切换为平缓语速的安抚女声）。
> 2. **软硬件结合的场景联动**：流式解析场景标签（Scene Markers），实现灯光、白噪音和故事文本的三位一体。

**Q3：为什么不直接做成 App？**
> **回答策略**：因为现代人的失眠，很大程度来源于“屏幕依赖”（蓝光、信息流）。做 App 是让用户继续看手机，而做成**纯语音交互的实体夜灯**，是让用户彻底放下手机，闭上眼睛，这是物理层面的“断舍离”，是真正的助眠赛道。

**Q4：为什么选择阿里云百炼（通义千问）全家桶？**
> **回答策略**：为了**集成度**和**速度**。在语音交互场景中，延迟是第一杀手。通义千问的 Flash 系列（ASR/TTS）在延迟表现上极其优异，且 `qwen-turbo` 作为意图解析器的性价比极高，完全满足了我们对毫秒级响应的严苛要求。
